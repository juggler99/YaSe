import 'package:flutter/material.dart';
import 'dart:ui';

/*
Size getDeviceScreenSize(BuildContext context) {
  // Screen size in density independent pixels
  var flutterView = FlutterView._(context);
  var rect = FlutterView.physicalGeometry();

  /*
  var screenWidth =
      (FlutterView.physicalSize.shortestSide / window.devicePixelRatio);
  var screenHeight =
      (window.physicalSize.longestSide / window.devicePixelRatio);

// Screen size in real pixels
  var screenWidthPixels = window.physicalSize.shortestSide;
  var screenHeightPixels = window.physicalSize.longestSide;
  */

  //return Size(screenWidthPixels, screenHeightPixels);
*/